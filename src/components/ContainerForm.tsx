import React from 'react'

interface ContainerFormProps {}

const ContainerForm: React.FC<ContainerFormProps> = ({}) => {
  return <div className="container__form"></div>
}

export default ContainerForm
